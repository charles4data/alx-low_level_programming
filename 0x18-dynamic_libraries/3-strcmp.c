#include "main.h"

/**
 * _strcmp - compares two strings
 * @s1: first string
 * @s2: second string
 * Return: return 0 if s1 and s2 are equal, another number if not.
 */

int _strcmp(char *s1, char *s2)
{
	int result = strcmp(s1, s2);

	return (result);
}
