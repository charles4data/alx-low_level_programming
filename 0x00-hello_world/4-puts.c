#include <stdio.h>

/**
 * entry point -main
 * function prints a string
 * function returns zero
 * Using printf now allowed
 */

int main(void)
{
	puts("Programming is like building a multilingual puzzle\n");
	return(0);
}
