#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen_recursion - returns the length of a string
 * @s: string to return
 * Return: Length of s.
 */

int _strlen_recursion(char *s)
{
	int len = strlen(s);

	return (len);
}
